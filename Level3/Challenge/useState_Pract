  const [color, setColor] = useState("pink")

    setColor("blue") 

const [color1, setColor1] = useState("pink")

    setColor(prevColor => prevColor === "pink" ? "blue" : pink)

    const [people, setPeople] = useState([
        {
            firstName: "John",
            lastName: "Smith"
        }
    ])
    
    setPeople(prevPeople => {
        return [
            ...prevPeople,
            {
                firstName:"Dakota",
                lastName:"Lail"
            }
        ]
    }) ;  

    const [colors, setColors] = setState(["pink", "blue"])

setColors(prevColors => [...prevColors, "green"])

const [countObject, setCountObject] = setState({
	count: 0
})

setCountObject(prevState=> 	({count: prevState.count + 1}))

const [person, setPerson] = useState({
    firstName: "John",
    lastName: "Smith"
})

setPerson(preState =>({
    ...prevState,
    age:30
}))

const [colors2, setColors2] = useState(["pink", "blue"])

setColors("green") 
//Overwrites the array of colors with just a single string